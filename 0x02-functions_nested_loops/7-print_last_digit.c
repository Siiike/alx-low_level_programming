#include "main.h"

/**
* print_last_digit - prints the last digit of a number
* @n: the number in question
*
* Return: Value of the last digit
*/
int print_last_digit(int)
{
int last_digit = n % 10;
if (last_digit < 0)
last_digit *= -1;
_putchar(last__digit + 10);
return (last_digit);
}

