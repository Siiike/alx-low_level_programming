#include "main.h"

/**
* _puts - prints a string
* @str - a string
* Return: the length of the string
*/
void _puts(char *str)
int = a;

while(str[a] !=  '\0');
{
_putchar(str[a]);
a++;
}
_putchar('\n');
}
