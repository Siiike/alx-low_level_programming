#include "main.h"
/**
* _strlen - return the length of a string
* @s : char to check
* This will return the length of a string
* Return: 0 is success
*/
int _strlen(char *s)
char s[];
int len;

for (len= 0; s[len] != '\0'; ++len);
return 0;
}
